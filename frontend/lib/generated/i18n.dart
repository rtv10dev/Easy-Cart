import 'dart:async';

import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

// ignore_for_file: non_constant_identifier_names
// ignore_for_file: camel_case_types
// ignore_for_file: prefer_single_quotes

// This file is automatically generated. DO NOT EDIT, all your changes would be lost.
class S implements WidgetsLocalizations {
  const S();

  static S current;

  static const GeneratedLocalizationsDelegate delegate =
    GeneratedLocalizationsDelegate();

  static S of(BuildContext context) => Localizations.of<S>(context, S);

  @override
  TextDirection get textDirection => TextDirection.ltr;

  String get back => "Back";
  String get cancel => "Cancel";
  String get check => "Check";
  String get confirm => "Confirm";
  String get delete => "Delete";
  String get deleteConfirmationAll => "Are you sure you want to delete all the products?";
  String get errorAddingProduct => "Error adding product";
  String get errorDeletingAll => "Error deleting all products";
  String get invalidName => "Invalid name";
  String get invalidProduct => "Invalid product name";
  String get list => "List";
  String get microphonePermission => "Microphone permission required to add products";
  String get nameQuestion => "What is your name?";
  String get next => "Next";
  String get productList => "Product list";
  String get quantity => "Quantity";
  String get save => "Save";
  String get talkNow => "Talk now";
  String deleteConfirmation(String productName) => "Are you sure you want to delete the product: $productName?";
}

class $en extends S {
  const $en();
}

class $es extends S {
  const $es();

  @override
  TextDirection get textDirection => TextDirection.ltr;

  @override
  String get cancel => "Cancelar";
  @override
  String get next => "Siguiente";
  @override
  String get quantity => "Cantidad";
  @override
  String get save => "Guardar";
  @override
  String get back => "Volver";
  @override
  String get check => "Completar";
  @override
  String get errorDeletingAll => "Error borrando todos los productos";
  @override
  String get list => "Lista";
  @override
  String get delete => "Eliminar";
  @override
  String get invalidProduct => "Nombre del producto no válido";
  @override
  String get nameQuestion => "¿Cómo te llamas?";
  @override
  String get invalidName => "Nombre no válido";
  @override
  String get confirm => "Confirmar";
  @override
  String get microphonePermission => "Es necesario que concedas el permiso de micrófono para añadir productos";
  @override
  String get errorAddingProduct => "Error al añadir el producto";
  @override
  String get talkNow => "Habla ahora";
  @override
  String get productList => "Lista de productos";
  @override
  String get deleteConfirmationAll => "¿Seguro que quieres eliminar todos los productos?";
  @override
  String deleteConfirmation(String productName) => "¿Seguro que quieres borrar el producto: $productName?";
}

class GeneratedLocalizationsDelegate extends LocalizationsDelegate<S> {
  const GeneratedLocalizationsDelegate();

  List<Locale> get supportedLocales {
    return const <Locale>[
      Locale("en", ""),
      Locale("es", ""),
    ];
  }

  LocaleListResolutionCallback listResolution({Locale fallback, bool withCountry = true}) {
    return (List<Locale> locales, Iterable<Locale> supported) {
      if (locales == null || locales.isEmpty) {
        return fallback ?? supported.first;
      } else {
        return _resolve(locales.first, fallback, supported, withCountry);
      }
    };
  }

  LocaleResolutionCallback resolution({Locale fallback, bool withCountry = true}) {
    return (Locale locale, Iterable<Locale> supported) {
      return _resolve(locale, fallback, supported, withCountry);
    };
  }

  @override
  Future<S> load(Locale locale) {
    final String lang = getLang(locale);
    if (lang != null) {
      switch (lang) {
        case "en":
          S.current = const $en();
          return SynchronousFuture<S>(S.current);
        case "es":
          S.current = const $es();
          return SynchronousFuture<S>(S.current);
        default:
          // NO-OP.
      }
    }
    S.current = const S();
    return SynchronousFuture<S>(S.current);
  }

  @override
  bool isSupported(Locale locale) => _isSupported(locale, true);

  @override
  bool shouldReload(GeneratedLocalizationsDelegate old) => false;

  ///
  /// Internal method to resolve a locale from a list of locales.
  ///
  Locale _resolve(Locale locale, Locale fallback, Iterable<Locale> supported, bool withCountry) {
    if (locale == null || !_isSupported(locale, withCountry)) {
      return fallback ?? supported.first;
    }

    final Locale languageLocale = Locale(locale.languageCode, "");
    if (supported.contains(locale)) {
      return locale;
    } else if (supported.contains(languageLocale)) {
      return languageLocale;
    } else {
      final Locale fallbackLocale = fallback ?? supported.first;
      return fallbackLocale;
    }
  }

  ///
  /// Returns true if the specified locale is supported, false otherwise.
  ///
  bool _isSupported(Locale locale, bool withCountry) {
    if (locale != null) {
      for (Locale supportedLocale in supportedLocales) {
        // Language must always match both locales.
        if (supportedLocale.languageCode != locale.languageCode) {
          continue;
        }

        // If country code matches, return this locale.
        if (supportedLocale.countryCode == locale.countryCode) {
          return true;
        }

        // If no country requirement is requested, check if this locale has no country.
        if (true != withCountry && (supportedLocale.countryCode == null || supportedLocale.countryCode.isEmpty)) {
          return true;
        }
      }
    }
    return false;
  }
}

String getLang(Locale l) => l == null
  ? null
  : l.countryCode != null && l.countryCode.isEmpty
    ? l.languageCode
    : l.toString();
